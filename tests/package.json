{
  "name": "@yuwen/tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@yuwen/chat-core": "workspace:*",
    "@yuwen/protocol": "workspace:*"
  }
}

